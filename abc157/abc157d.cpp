#include <bits/stdc++.h>
using namespace std;

int main()
{
    /* ------- 宣言と入力 ------- */

    int N, M, K;

    cin >> N >> M >> K;

    int A[M], B[M];

    for (int i = 0; i < M; i++)
    {
        cin >> A[i] >> B[i];
    }

    int C[M], D[M];

    for (int i = 0; i < K; i++)
    {
        cin >> C[i] >> D[i];
    }

    /* ------- 計算 ------- */

    

    int ans;

    cout << ans << endl;

    return 0;
}